# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
build/
.fvm/
flutter_*.png
linked_*.ds
unlinked.ds
unlinked_spec.ds

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws
.metadata

# OS
.DS_Store
Thumbs.db

# Generated
ios/Pods/
ios/.symlinks/
ios/Flutter/Generated.xcconfig
ios/Flutter/flutter_export_environment.sh
android/.gradle/
android/local.properties
android/key.properties
android/app/.cxx/
android/app/release/
*.jks
*.keystore

# Coverage
coverage/

# Local secrets
.env
.env.local

# Firebase Admin SDK private key — NEVER commit, used only server-side
*firebase-adminsdk*.json
serviceAccountKey.json
firebase-service-account.json

# Note: google-services.json, GoogleService-Info.plist, firebase_options.dart
# CAN be committed safely (they hold only public app identifiers restricted
# to specific bundle IDs). They are required for clones to build the app.
